# build products: everything here can be deleted and rebuilt
/cache/
/out/
/dist/
/qemu-build/

# the mingw cross-build keeps its own list, in win64/.gitignore

# Electron
/electron/node_modules/

__pycache__/
*.pyc
